<section id="pagos" class="bg-white dark:bg-gray-900 py-16 px-4 text-center">
    <h2 class="text-3xl font-bold text-linknet dark:text-linknet-accent mb-6">
      Paga tu cuenta
    </h2>
    <p class="text-gray-600 dark:text-gray-400 mb-8 max-w-xl mx-auto">
      Ingresa tu RUT sin puntos y con guion para revisar y pagar tu boleta en línea.
    </p>
  
    <form id="form-pago" class="max-w-md mx-auto flex flex-col sm:flex-row items-center gap-4">
      <input
        type="text"
        id="rut"
        name="rut"
        placeholder="Ej: 12345678-9"
        class="w-full px-4 py-3 border rounded-xl focus:outline-none focus:ring-2 focus:ring-linknet-accent"
        required
      />
      <a href="#pagos"
      <button
        <
        type="submit"
        class="bg-linknet text-white px-6 py-3 rounded-xl hover:bg-linknet-accent transition"
      >
        Pagar mi cuenta
      </button>
    </form>
  
    <script is:inline>
      document.getElementById("form-pago")?.addEventListener("submit", function (e) {
        e.preventDefault();
        const rut = document.getElementById("rut")?.value.trim();
        if (rut) {
          // Redirige al sistema de pago
          window.location.href = `https://linknetspa.cl/pago?rut=${encodeURIComponent(rut)}`;
        }
      });
    </script>
  </section>
  